name: gnps
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - llvm-openmp  # [osx]
  - libgomp  # [linux]
  - pip
  - pip:
    - -r requirements.txt
    - -r local_runner/requirements.txt